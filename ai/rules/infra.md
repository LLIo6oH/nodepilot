# Infra Rules

- Docker Compose is the default local/runtime path.
- Keep deployments AWS EC2 free-tier compatible.
- Prefer simplicity and operability over scalability for MVP.
- Kubernetes is intentionally out of MVP scope.
